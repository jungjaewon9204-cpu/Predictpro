const router = require('express').Router();

// POST /api/premium/activate
router.post('/activate', (req, res) => {  
  res.json({message: 'premium activate placeholder'});  
});

module.exports = router;  
